// Universal input constants shared across all scenes.
// Cutscene / paginated-text systems must import from here so the
// advance key is defined in exactly one place.

export const TEXT_ADVANCE_KEY = Phaser.Input.Keyboard.KeyCodes.M
export const TEXT_SKIP_KEY    = Phaser.Input.Keyboard.KeyCodes.SPACE
export const HUD_LEGEND_KEY   = Phaser.Input.Keyboard.KeyCodes.H
